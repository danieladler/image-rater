<div class="row">
  <div class="add-photo-form col-lg-6 col-lg-push-3 col-sm-12 ">
      <div class="add-photo-form-header">
        <h1>Add Photo</h1>
      </div>
      <%= form_for @photo, url: add_photo_path do |f| %>
      <div class="form-group signup-input">
        <%= f.label      :title %><br>
        <%= f.text_field :title %>
      </div>
      <div class="form-group signup-input">
        <%= f.label      :description %><br>
        <%= f.text_field :description %>
      </div>
      <div class="form-group signup-input">
        <%= f.label      :image_file %><br>
        <%= f.file_field :image_file %>
      </div>
      <div>
        <%= f.submit id: :submit_new_photo%>
      </div>
    <% end %>
  </div>
</div>

<div class="errors-container">
  <% if @photo %>
    <% if @photo.errors.any? %>
      <% @photo.errors.each do |column, message| %>
        <ul>
          <li>
            <strong><%= column.capitalize %>:</strong> <%= message %>
          </li>
        </ul>
      <% end %>
    <% end %>
  <% end %>
</div>
