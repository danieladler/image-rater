<nav class="navbar navbar-default navbar-fixed-top">
  <!-- flash notice logic goes here, or possibly in its own partial -->
  <div class="container">
    <div class="navbar-header">
      <ul class="nav navbar-nav">
        <li><%= link_to "PhotoSquid", root_path, class:"navbar-brand" %></li>
        <li><%= link_to "Rankings", ranked_path %></li>
        <% if random_photo %>
          <li><%= link_to "Random", photo_path(random_photo) %></li>
        <% else %>
          <li><a>No photos left to rate!</a></li>
        <% end %>
      </ul>

      <button class="navbar-toggle collapsed" data-toggle="collapse" data-target=".menu-wrapper">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>

    <div class="menu-wrapper collapse navbar-collapse">
      <div class="navbar-right">
        <ul class="nav navbar-nav">
        <% if @current_user %>
          <li><a id="greeting">Hello, <span class="username"><%= @current_user.username%></span></a></li>
          <li><a id="avatar-link-centerer"> <%= image_tag @current_user.avatar, :class=>"avatar avatar-header" %></a>
          <li><%= link_to "My Account"    , user_path(@current_user.id)%> </li>
          <li><%= link_to "Sign Out"      , sign_out_path%>            </li>
        </ul>
        <% else %>
          <li><%= render "layouts/sign_in_form" %></li>
          <a><button class="btn btn-success btn-md", id="create-acct-btn"><%= link_to "Create Account", sign_up_path %> </button></a>
        <% end %>
      </div>
    </div>
  </div>
</nav>
