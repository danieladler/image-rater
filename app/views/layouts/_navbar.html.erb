<nav class="navbar navbar-default navbar-fixed-top">
  <!-- flash notice logic goes here, or possibly in its own partial -->
  <div class="container">
    <div class="navbar-header">
      <ul class="nav navbar-nav">
        <li><%= link_to "Home", root_path, class:"navbar-brand" %></li>
        <li><%= link_to "Random", photo_path(Photo.rate_random(@current_user.id)) %></li>
      </ul>

      <button class="navbar-toggle collapsed" data-toggle="collapse" data-target=".menu-wrapper">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>

    <div class="menu-wrapper collapse navbar-collapse">
      <div class="navbar-right">
        <ul class="nav navbar-nav">
        <% if @current_user %>
          <li><a id="greeting">Hello, <strong><%= @current_user.username%></strong></a></li>
          <li><%= link_to "My Account"    , user_path(@current_user.id)%> </li>
          <li><%= link_to "Sign Out"      , sign_out_path%>            </li>
        </ul>
        <% else %>
          <li><%= render "layouts/sign_in_form" %></li>
          <button class="btn btn-success btn-md", id="create-acct-btn"><%= link_to "Create Account", sign_up_path %> </button>
        <% end %>
      </div>
    </div>
  </div>
</nav>
